import CRC from "#CRC";

describe("CRC 单元测试", () => {
    describe("CRC16/USB", () => {
        it ("计算", done => {
            done();
        });
        it ("校验", done => {
            done();
        });
    });
    describe("CRC16/XMODEM", () => {
        it ("计算", done => {
            done();
        });
        it ("校验", done => {
            done();
        });
    });
    describe("CRC32", () => {
        it ("计算", done => {
            done();
        });
        it ("校验", done => {
            done();
        });
    });
});